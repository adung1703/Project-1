#include <bits/stdc++.h>
using namespace std;
struct Node {
	string name;
	vector<Node*> children;
	Node(string s) : name(s) {}
};

void insert(Node* root, pair<string, string> cp) {
	if (cp.)
}

int main(){
	vector<pair<string, string>> v;
	pair<string, string> cp;
	cin >> cp.first >> cp.second;
	v.push_back(cp);
	while (cin) {
		cin.ignore();
		cin >> cp.first;
		if (cp.first == "***") break;
		cin >> cp.second;
		v.push_back(cp);
	}
	
	Node *root;

}

